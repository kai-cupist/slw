---
phase: 02-prompts-submissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/001_create_prompts.sql
  - migrations/002_create_submissions.sql
  - migrations/003_seed_prompts.sql
  - server/src/common/guards/user-id.guard.ts
  - server/src/common/decorators/user-id.decorator.ts
  - server/src/common/dto/pagination.dto.ts
  - server/src/common/interfaces/paginated.interface.ts
autonomous: true
requirements: [PROMPT-03, PROMPT-04]

must_haves:
  truths:
    - "prompts 테이블이 DB에 존재하고, category와 difficulty에 CHECK constraint가 적용되어 있다"
    - "submissions 테이블이 DB에 존재하고, prompt_id FK, status CHECK, deleted_at 컬럼이 있다"
    - "시드 데이터 25-30개 주제가 5개 카테고리 x 3단계 난이도로 DB에 삽입되어 있다"
    - "X-User-Id 헤더가 없는 요청은 400 에러로 거부된다"
    - "페이지네이션 DTO가 page/limit 쿼리 파라미터를 타입 변환 및 검증한다"
  artifacts:
    - path: "migrations/001_create_prompts.sql"
      provides: "prompts 테이블 DDL + 인덱스"
      contains: "CREATE TABLE prompts"
    - path: "migrations/002_create_submissions.sql"
      provides: "submissions 테이블 DDL + 인덱스"
      contains: "CREATE TABLE submissions"
    - path: "migrations/003_seed_prompts.sql"
      provides: "시드 데이터 INSERT"
      contains: "INSERT INTO prompts"
    - path: "server/src/common/guards/user-id.guard.ts"
      provides: "X-User-Id 헤더 검증 가드"
      exports: ["UserIdGuard"]
    - path: "server/src/common/decorators/user-id.decorator.ts"
      provides: "X-User-Id 값 추출 데코레이터"
      exports: ["UserId"]
    - path: "server/src/common/dto/pagination.dto.ts"
      provides: "공통 페이지네이션 쿼리 DTO"
      exports: ["PaginationDto"]
    - path: "server/src/common/interfaces/paginated.interface.ts"
      provides: "페이지네이션 응답 인터페이스"
      exports: ["PaginatedResponse"]
  key_links:
    - from: "server/src/common/guards/user-id.guard.ts"
      to: "X-User-Id 요청 헤더"
      via: "request.headers['x-user-id'] 읽기 + 빈 문자열/undefined 검증"
      pattern: "headers\\['x-user-id'\\]"
    - from: "migrations/002_create_submissions.sql"
      to: "migrations/001_create_prompts.sql"
      via: "REFERENCES prompts(id) FK"
      pattern: "REFERENCES prompts"
---

<objective>
Phase 2의 DB 스키마(prompts, submissions 테이블)와 시드 데이터를 마이그레이션 SQL로 생성하고,
후속 Plan에서 사용할 공통 인프라(X-User-Id 가드/데코레이터, 페이지네이션 DTO/인터페이스)를 구축한다.

Purpose: Phase 2의 모든 API 엔드포인트가 의존하는 DB 테이블과 공통 코드를 선행 구축한다.
Output: 마이그레이션 SQL 3개 + 공통 인프라 TypeScript 4개
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompts-submissions/02-CONTEXT.md
@.planning/phases/02-prompts-submissions/02-RESEARCH.md

<interfaces>
<!-- Phase 1에서 확립된 핵심 인터페이스 -- executor가 참조해야 할 계약 -->

From server/src/database/database.service.ts:

```typescript
@Injectable()
export class DatabaseService implements OnModuleDestroy {
  async query<T extends QueryResultRow = Record<string, unknown>>(
    text: string,
    params?: unknown[],
  ): Promise<T[]>;

  async queryOne<T extends QueryResultRow = Record<string, unknown>>(
    text: string,
    params?: unknown[],
  ): Promise<T | null>;

  async withTransaction<T>(
    callback: (client: PoolClient) => Promise<T>,
  ): Promise<T>;
}
```

From server/src/common/interfaces/api-response.interface.ts:

```typescript
export interface ApiSuccessResponse<T> {
  success: true;
  data: T;
}

export interface ApiErrorResponse {
  success: false;
  error: {
    code: string;
    message: string;
    details?: any;
  };
}
```

From server/src/common/filters/http-exception.filter.ts:

```typescript
// HttpExceptionFilter는 HttpException의 status에서 HttpStatus[status] 문자열을 code로 사용
// 예: NotFoundException → code: "NOT_FOUND", BadRequestException → code: "BAD_REQUEST"
// message 배열(ValidationPipe 에러)은 details 필드로 변환
```

기존 마이그레이션 파일:

```
migrations/000_create_schema_migrations.sql  # 이미 존재
```

기존 프로젝트 구조:

```
server/src/
├── common/
│   ├── filters/http-exception.filter.ts    # 기존
│   ├── interceptors/response.interceptor.ts # 기존
│   └── interfaces/api-response.interface.ts # 기존
├── database/
│   ├── database.module.ts      # @Global() — DatabaseService 전역 주입
│   ├── database.service.ts
│   └── database.constants.ts
├── migration/
│   ├── migration.service.ts    # migrations/ 폴더 SQL 파일 순서대로 실행
│   ├── migration.module.ts
│   └── run-migrations.ts
├── app.module.ts
├── app.controller.ts
└── main.ts
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB 마이그레이션 SQL 작성 (prompts + submissions 테이블 + 시드 데이터)</name>
  <files>
    migrations/001_create_prompts.sql
    migrations/002_create_submissions.sql
    migrations/003_seed_prompts.sql
  </files>
  <read_first>
    migrations/000_create_schema_migrations.sql
    server/src/migration/migration.service.ts
    .planning/phases/02-prompts-submissions/02-CONTEXT.md
    .planning/phases/02-prompts-submissions/02-RESEARCH.md
  </read_first>
  <action>
    3개의 마이그레이션 SQL 파일을 작성한다. 기존 000 파일의 네이밍 패턴(NNN_description.sql)을 따른다.

    **001_create_prompts.sql** (per D-01, D-02, D-04, D-05):
    ```sql
    CREATE TABLE prompts (
        id SERIAL PRIMARY KEY,
        title VARCHAR(200) NOT NULL,
        description TEXT NOT NULL,
        category VARCHAR(50) NOT NULL,
        difficulty VARCHAR(20) NOT NULL,
        created_at TIMESTAMP NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
        CONSTRAINT chk_difficulty CHECK (difficulty IN ('beginner', 'intermediate', 'advanced')),
        CONSTRAINT chk_category CHECK (category IN ('일기', '편지', '감상문', '설명문', '논설문'))
    );

    CREATE INDEX idx_prompts_category ON prompts (category);
    CREATE INDEX idx_prompts_difficulty ON prompts (difficulty);
    ```

    **002_create_submissions.sql** (per D-01, D-03, D-06, D-08, D-14):
    ```sql
    CREATE TABLE submissions (
        id SERIAL PRIMARY KEY,
        prompt_id INTEGER NOT NULL REFERENCES prompts(id),
        user_id VARCHAR(100) NOT NULL,
        content TEXT NOT NULL DEFAULT '',
        status VARCHAR(20) NOT NULL DEFAULT 'draft',
        created_at TIMESTAMP NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
        deleted_at TIMESTAMP,
        CONSTRAINT chk_status CHECK (status IN ('draft', 'submitted'))
    );

    CREATE INDEX idx_submissions_user_id ON submissions (user_id) WHERE deleted_at IS NULL;
    CREATE INDEX idx_submissions_prompt_id ON submissions (prompt_id) WHERE deleted_at IS NULL;
    CREATE INDEX idx_submissions_user_prompt_draft ON submissions (user_id, prompt_id)
        WHERE status = 'draft' AND deleted_at IS NULL;
    ```

    **003_seed_prompts.sql** (per D-15, D-16, D-17):
    5개 카테고리(일기, 편지, 감상문, 설명문, 논설문) x 3단계 난이도(beginner, intermediate, advanced) = 최소 25개, 최대 30개 주제를 INSERT한다.
    각 주제는 한국어 초등~중등 수준의 실제 쓰기 주제여야 한다. 예시:
    - (일기, beginner): '오늘 있었던 일', '오늘의 기분과 그 이유를 적어 보세요.'
    - (편지, intermediate): '선생님께 감사 편지', '존경하는 선생님께 감사의 마음을 전하는 편지를 써 보세요.'
    - (논설문, advanced): '스마트폰 사용 나이 제한', '스마트폰 사용 최소 나이를 정해야 하는지에 대한 여러분의 의견을 논리적으로 서술하세요.'

    INSERT문 형식:
    ```sql
    INSERT INTO prompts (title, description, category, difficulty) VALUES
    ('제목', '상세 설명', '카테고리', '난이도'),
    ...;
    ```

  </action>
  <verify>
    <automated>cd /Users/cupist/workspace/slw && docker compose exec api npm run migration:run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - migrations/001_create_prompts.sql 파일이 존재하고 "CREATE TABLE prompts" 문자열을 포함한다
    - migrations/001_create_prompts.sql에 "CONSTRAINT chk_difficulty CHECK" 문자열이 있다
    - migrations/001_create_prompts.sql에 "CONSTRAINT chk_category CHECK" 문자열이 있다
    - migrations/001_create_prompts.sql에 "CREATE INDEX idx_prompts_category" 문자열이 있다
    - migrations/002_create_submissions.sql 파일이 존재하고 "CREATE TABLE submissions" 문자열을 포함한다
    - migrations/002_create_submissions.sql에 "REFERENCES prompts(id)" 문자열이 있다
    - migrations/002_create_submissions.sql에 "CONSTRAINT chk_status CHECK" 문자열이 있다
    - migrations/002_create_submissions.sql에 "deleted_at TIMESTAMP" 문자열이 있다
    - migrations/002_create_submissions.sql에 "WHERE deleted_at IS NULL" (partial index)이 있다
    - migrations/003_seed_prompts.sql 파일이 존재하고 "INSERT INTO prompts" 문자열을 포함한다
    - migrations/003_seed_prompts.sql에 5개 카테고리(일기, 편지, 감상문, 설명문, 논설문)가 모두 포함되어 있다
    - migrations/003_seed_prompts.sql에 3개 난이도(beginner, intermediate, advanced)가 모두 포함되어 있다
    - docker compose exec api npm run migration:run이 에러 없이 완료된다
    - docker compose exec api sh -c "PGPASSWORD=\$DB_PASSWORD psql -h \$DB_HOST -U \$DB_USER -d \$DB_NAME -c 'SELECT COUNT(*) FROM prompts'" 결과가 25 이상이다
  </acceptance_criteria>
  <done>3개 마이그레이션 파일이 순서대로 실행되어 prompts/submissions 테이블이 생성되고, 25-30개 시드 데이터가 삽입된 상태</done>
</task>

<task type="auto">
  <name>Task 2: 공통 인프라 코드 (UserIdGuard, UserId 데코레이터, PaginationDto, PaginatedResponse)</name>
  <files>
    server/src/common/guards/user-id.guard.ts
    server/src/common/decorators/user-id.decorator.ts
    server/src/common/dto/pagination.dto.ts
    server/src/common/interfaces/paginated.interface.ts
  </files>
  <read_first>
    server/src/common/filters/http-exception.filter.ts
    server/src/common/interceptors/response.interceptor.ts
    server/src/common/interfaces/api-response.interface.ts
    server/src/main.ts
    .planning/phases/02-prompts-submissions/02-RESEARCH.md
  </read_first>
  <action>
    Phase 2의 모든 API에서 공유하는 4개 공통 코드를 작성한다.

    **1. server/src/common/guards/user-id.guard.ts** (per D-12, D-13):
    ```typescript
    import { Injectable, CanActivate, ExecutionContext, BadRequestException } from '@nestjs/common';

    @Injectable()
    export class UserIdGuard implements CanActivate {
      canActivate(context: ExecutionContext): boolean {
        const request = context.switchToHttp().getRequest();
        const userId = request.headers['x-user-id'];

        if (!userId || typeof userId !== 'string' || userId.trim() === '') {
          throw new BadRequestException('X-User-Id 헤더가 필요합니다');
        }

        return true;
      }
    }
    ```
    - 전역 가드가 아님! submissions 컨트롤러에만 @UseGuards(UserIdGuard)로 적용한다 (prompts는 공개 데이터이므로 가드 불필요).
    - 빈 문자열도 거부한다.

    **2. server/src/common/decorators/user-id.decorator.ts** (per D-12):
    ```typescript
    import { createParamDecorator, ExecutionContext } from '@nestjs/common';

    export const UserId = createParamDecorator(
      (data: unknown, ctx: ExecutionContext): string => {
        const request = ctx.switchToHttp().getRequest();
        return request.headers['x-user-id'] as string;
      },
    );
    ```

    **3. server/src/common/dto/pagination.dto.ts** (per D-09, D-10, D-11):
    ```typescript
    import { ApiPropertyOptional } from '@nestjs/swagger';
    import { Type } from 'class-transformer';
    import { IsInt, IsOptional, Max, Min } from 'class-validator';

    export class PaginationDto {
      @ApiPropertyOptional({ description: '페이지 번호', default: 1, minimum: 1 })
      @IsOptional()
      @Type(() => Number)
      @IsInt()
      @Min(1)
      page?: number = 1;

      @ApiPropertyOptional({ description: '페이지당 항목 수', default: 10, minimum: 1, maximum: 100 })
      @IsOptional()
      @Type(() => Number)
      @IsInt()
      @Min(1)
      @Max(100)
      limit?: number = 10;
    }
    ```

    **4. server/src/common/interfaces/paginated.interface.ts** (per D-11):
    ```typescript
    export interface PaginatedResponse<T> {
      items: T[];
      total: number;
      page: number;
      limit: number;
      totalPages: number;
    }
    ```

  </action>
  <verify>
    <automated>cd /Users/cupist/workspace/slw/server && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - server/src/common/guards/user-id.guard.ts가 존재하고 "export class UserIdGuard" 문자열을 포함한다
    - server/src/common/guards/user-id.guard.ts에 "headers['x-user-id']" 문자열이 있다
    - server/src/common/guards/user-id.guard.ts에 "BadRequestException" 문자열이 있다
    - server/src/common/guards/user-id.guard.ts에 "userId.trim() === ''" 패턴이 있다
    - server/src/common/decorators/user-id.decorator.ts가 존재하고 "export const UserId" 문자열을 포함한다
    - server/src/common/decorators/user-id.decorator.ts에 "createParamDecorator" 문자열이 있다
    - server/src/common/dto/pagination.dto.ts가 존재하고 "export class PaginationDto" 문자열을 포함한다
    - server/src/common/dto/pagination.dto.ts에 "page?: number = 1" 패턴이 있다
    - server/src/common/dto/pagination.dto.ts에 "limit?: number = 10" 패턴이 있다
    - server/src/common/interfaces/paginated.interface.ts가 존재하고 "export interface PaginatedResponse" 문자열을 포함한다
    - server/src/common/interfaces/paginated.interface.ts에 "totalPages: number" 문자열이 있다
    - npx tsc --noEmit 명령이 에러 없이 완료된다
  </acceptance_criteria>
  <done>UserIdGuard, UserId 데코레이터, PaginationDto, PaginatedResponse 인터페이스가 생성되어 TypeScript 컴파일이 성공하고, 후속 Plan에서 import하여 사용할 수 있는 상태</done>
</task>

</tasks>

<verification>
1. 3개 마이그레이션 파일이 migrations/ 폴더에 존재한다 (001, 002, 003)
2. docker compose exec api npm run migration:run으로 마이그레이션이 성공한다
3. DB에 prompts 테이블(CHECK constraint 포함)과 submissions 테이블(FK, CHECK, partial index 포함)이 생성된다
4. DB에 25-30개 시드 데이터가 삽입된다
5. 공통 인프라 4개 파일이 server/src/common/ 하위에 존재한다
6. TypeScript 컴파일(tsc --noEmit)이 에러 없이 통과한다
</verification>

<success_criteria>

- prompts, submissions 테이블이 DB에 존재하고 제약조건(CHECK, FK, partial index)이 적용된 상태
- 시드 데이터 25개 이상이 prompts 테이블에 삽입된 상태
- UserIdGuard, UserId, PaginationDto, PaginatedResponse가 import 가능한 상태
- TypeScript 컴파일 에러 없음
  </success_criteria>

<output>
After completion, create `.planning/phases/02-prompts-submissions/02-01-SUMMARY.md`
</output>
