---
phase: 02-prompts-submissions
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - server/src/prompts/prompts.module.ts
  - server/src/prompts/prompts.controller.ts
  - server/src/prompts/prompts.service.ts
  - server/src/prompts/prompts.repository.ts
  - server/src/prompts/dto/get-prompts.dto.ts
  - server/src/app.module.ts
autonomous: true
requirements: [PROMPT-01, PROMPT-02]

must_haves:
  truths:
    - "GET /prompts 요청 시 주제 목록이 페이지네이션되어 반환된다"
    - "GET /prompts?category=일기&difficulty=beginner로 필터링이 가능하다"
    - "GET /prompts/:id 요청 시 해당 주제의 상세 정보가 반환된다"
    - "존재하지 않는 주제 ID로 요청하면 404 에러가 반환된다"
    - "주제 API에는 X-User-Id 가드가 적용되지 않는다 (공개 데이터)"
  artifacts:
    - path: "server/src/prompts/prompts.repository.ts"
      provides: "Raw SQL로 prompts 테이블 조회"
      exports: ["PromptsRepository"]
    - path: "server/src/prompts/prompts.service.ts"
      provides: "주제 비즈니스 로직"
      exports: ["PromptsService"]
    - path: "server/src/prompts/prompts.controller.ts"
      provides: "GET /prompts, GET /prompts/:id 라우트"
      exports: ["PromptsController"]
    - path: "server/src/prompts/prompts.module.ts"
      provides: "Prompts NestJS 모듈"
      exports: ["PromptsModule"]
    - path: "server/src/prompts/dto/get-prompts.dto.ts"
      provides: "주제 필터링 + 페이지네이션 DTO"
      exports: ["GetPromptsDto", "Difficulty", "Category"]
  key_links:
    - from: "server/src/prompts/prompts.repository.ts"
      to: "server/src/database/database.service.ts"
      via: "DatabaseService 주입, this.db.query/queryOne 호출"
      pattern: "this\\.db\\.(query|queryOne)"
    - from: "server/src/prompts/prompts.controller.ts"
      to: "server/src/prompts/prompts.service.ts"
      via: "PromptsService 주입, this.promptsService 호출"
      pattern: "this\\.promptsService"
    - from: "server/src/prompts/prompts.service.ts"
      to: "server/src/prompts/prompts.repository.ts"
      via: "PromptsRepository 주입"
      pattern: "this\\.promptsRepository"
    - from: "server/src/app.module.ts"
      to: "server/src/prompts/prompts.module.ts"
      via: "imports 배열에 PromptsModule 추가"
      pattern: "PromptsModule"
---

<objective>
쓰기 주제(prompts) 읽기 전용 API를 구현한다. GET /prompts (목록, 필터링, 페이지네이션)와 GET /prompts/:id (상세) 두 엔드포인트를 제공한다.

Purpose: 사용자가 쓰기 주제를 탐색하고 선택할 수 있는 API를 제공한다.
Output: PromptsModule (Repository + Service + Controller + DTO) + AppModule 등록
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompts-submissions/02-CONTEXT.md
@.planning/phases/02-prompts-submissions/02-RESEARCH.md
@.planning/phases/02-prompts-submissions/02-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 공통 인프라 -- executor가 import하여 사용해야 할 계약 -->

From server/src/common/dto/pagination.dto.ts:

```typescript
export class PaginationDto {
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  page?: number = 1;

  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  @Max(100)
  limit?: number = 10;
}
```

From server/src/common/interfaces/paginated.interface.ts:

```typescript
export interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  limit: number;
  totalPages: number;
}
```

From server/src/database/database.service.ts:

```typescript
@Injectable()
export class DatabaseService {
  async query<T>(text: string, params?: unknown[]): Promise<T[]>;
  async queryOne<T>(text: string, params?: unknown[]): Promise<T | null>;
}
```

From server/src/common/interfaces/api-response.interface.ts:

```typescript
export interface ApiSuccessResponse<T> {
  success: true;
  data: T;
}
export interface ApiErrorResponse {
  success: false;
  error: { code: string; message: string; details?: any };
}
```

DB 스키마 (migrations/001_create_prompts.sql):

```sql
CREATE TABLE prompts (
    id SERIAL PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    description TEXT NOT NULL,
    category VARCHAR(50) NOT NULL,
    difficulty VARCHAR(20) NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);
```

기존 app.module.ts:

```typescript
@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    DatabaseModule,
    MigrationModule,
  ],
  controllers: [AppController],
})
export class AppModule {}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PromptsRepository + PromptsService + GetPromptsDto 구현</name>
  <files>
    server/src/prompts/prompts.repository.ts
    server/src/prompts/prompts.service.ts
    server/src/prompts/dto/get-prompts.dto.ts
  </files>
  <read_first>
    server/src/database/database.service.ts
    server/src/common/dto/pagination.dto.ts
    server/src/common/interfaces/paginated.interface.ts
    migrations/001_create_prompts.sql
    .planning/phases/02-prompts-submissions/02-RESEARCH.md
  </read_first>
  <action>
    3개 파일을 생성한다.

    **1. server/src/prompts/dto/get-prompts.dto.ts** (per D-18, D-20):
    PaginationDto를 상속하여 category와 difficulty 필터링 파라미터를 추가한다.

    ```typescript
    import { ApiPropertyOptional } from '@nestjs/swagger';
    import { IsEnum, IsOptional } from 'class-validator';
    import { PaginationDto } from '../../common/dto/pagination.dto';

    export enum Difficulty {
      BEGINNER = 'beginner',
      INTERMEDIATE = 'intermediate',
      ADVANCED = 'advanced',
    }

    export enum Category {
      DIARY = '일기',
      LETTER = '편지',
      REVIEW = '감상문',
      EXPLANATION = '설명문',
      ARGUMENT = '논설문',
    }

    export class GetPromptsDto extends PaginationDto {
      @ApiPropertyOptional({ description: '카테고리 필터', enum: Category })
      @IsOptional()
      @IsEnum(Category)
      category?: Category;

      @ApiPropertyOptional({ description: '난이도 필터', enum: Difficulty })
      @IsOptional()
      @IsEnum(Difficulty)
      difficulty?: Difficulty;
    }
    ```

    **2. server/src/prompts/prompts.repository.ts**:
    DatabaseService를 주입받아 prompts 테이블에 Raw SQL 쿼리를 실행한다.

    ```typescript
    @Injectable()
    export class PromptsRepository {
      constructor(private readonly db: DatabaseService) {}

      async findAll(
        filters: { category?: string; difficulty?: string },
        offset: number,
        limit: number,
      ): Promise<{ rows: Prompt[]; total: number }> {
        // 동적 WHERE 절 구성 (paramIndex 카운터 방식)
        const conditions: string[] = [];
        const params: unknown[] = [];
        let paramIndex = 1;

        if (filters.category) {
          conditions.push(`category = $${paramIndex++}`);
          params.push(filters.category);
        }
        if (filters.difficulty) {
          conditions.push(`difficulty = $${paramIndex++}`);
          params.push(filters.difficulty);
        }

        const whereClause = conditions.length > 0
          ? `WHERE ${conditions.join(' AND ')}`
          : '';

        // COUNT와 SELECT에 동일한 WHERE 절 공유 (Pitfall 2 방지)
        const countQuery = `SELECT COUNT(*) as total FROM prompts ${whereClause}`;
        const dataQuery = `
          SELECT id, title, description, category, difficulty, created_at, updated_at
          FROM prompts ${whereClause}
          ORDER BY created_at DESC
          LIMIT $${paramIndex++} OFFSET $${paramIndex}
        `;

        const [countResult, rows] = await Promise.all([
          this.db.queryOne<{ total: string }>(countQuery, [...params]),
          this.db.query<Prompt>(dataQuery, [...params, limit, offset]),
        ]);

        return { rows, total: parseInt(countResult?.total ?? '0', 10) };
      }

      async findOneById(id: number): Promise<Prompt | null> {
        return this.db.queryOne<Prompt>(
          'SELECT id, title, description, category, difficulty, created_at, updated_at FROM prompts WHERE id = $1',
          [id],
        );
      }
    }
    ```

    Prompt 타입은 repository 파일 상단에 interface로 정의한다:
    ```typescript
    export interface Prompt {
      id: number;
      title: string;
      description: string;
      category: string;
      difficulty: string;
      created_at: Date;
      updated_at: Date;
    }
    ```

    **3. server/src/prompts/prompts.service.ts** (per D-10):
    Repository를 주입받아 비즈니스 로직(페이지네이션 계산, 404 처리)을 담당한다.

    ```typescript
    @Injectable()
    export class PromptsService {
      constructor(private readonly promptsRepository: PromptsRepository) {}

      async findAll(dto: GetPromptsDto): Promise<PaginatedResponse<Prompt>> {
        const page = dto.page ?? 1;
        const limit = dto.limit ?? 10;
        const offset = (page - 1) * limit;

        const { rows, total } = await this.promptsRepository.findAll(
          { category: dto.category, difficulty: dto.difficulty },
          offset,
          limit,
        );

        return {
          items: rows,
          total,
          page,
          limit,
          totalPages: Math.ceil(total / limit),
        };
      }

      async findOne(id: number): Promise<Prompt> {
        const prompt = await this.promptsRepository.findOneById(id);
        if (!prompt) {
          throw new NotFoundException('주제를 찾을 수 없습니다');
        }
        return prompt;
      }
    }
    ```

  </action>
  <verify>
    <automated>cd /Users/cupist/workspace/slw/server && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - server/src/prompts/dto/get-prompts.dto.ts가 존재하고 "export class GetPromptsDto extends PaginationDto" 문자열을 포함한다
    - server/src/prompts/dto/get-prompts.dto.ts에 "export enum Difficulty" 문자열이 있다
    - server/src/prompts/dto/get-prompts.dto.ts에 "export enum Category" 문자열이 있다
    - server/src/prompts/dto/get-prompts.dto.ts에 "DIARY = '일기'" 문자열이 있다
    - server/src/prompts/prompts.repository.ts가 존재하고 "export class PromptsRepository" 문자열을 포함한다
    - server/src/prompts/prompts.repository.ts에 "export interface Prompt" 문자열이 있다
    - server/src/prompts/prompts.repository.ts에 "async findAll(" 문자열이 있다
    - server/src/prompts/prompts.repository.ts에 "async findOneById(" 문자열이 있다
    - server/src/prompts/prompts.repository.ts에 "parseInt(countResult?.total" 문자열이 있다 (Pitfall 6 방지)
    - server/src/prompts/prompts.repository.ts에 "paramIndex" 문자열이 있다 (Pitfall 3 방지)
    - server/src/prompts/prompts.service.ts가 존재하고 "export class PromptsService" 문자열을 포함한다
    - server/src/prompts/prompts.service.ts에 "NotFoundException" 문자열이 있다
    - server/src/prompts/prompts.service.ts에 "PaginatedResponse" 문자열이 있다
    - server/src/prompts/prompts.service.ts에 "Math.ceil(total / limit)" 문자열이 있다
    - npx tsc --noEmit 명령이 에러 없이 완료된다
  </acceptance_criteria>
  <done>PromptsRepository(findAll, findOneById), PromptsService(findAll with 페이지네이션, findOne with 404), GetPromptsDto(category/difficulty 필터)가 구현되어 TypeScript 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: PromptsController + PromptsModule + AppModule 등록 및 API 동작 검증</name>
  <files>
    server/src/prompts/prompts.controller.ts
    server/src/prompts/prompts.module.ts
    server/src/app.module.ts
  </files>
  <read_first>
    server/src/prompts/prompts.service.ts
    server/src/prompts/prompts.repository.ts
    server/src/prompts/dto/get-prompts.dto.ts
    server/src/common/dto/pagination.dto.ts
    server/src/app.module.ts
    server/src/main.ts
  </read_first>
  <action>
    3개 파일을 생성/수정한다.

    **1. server/src/prompts/prompts.controller.ts** (per D-18, D-20):
    ```typescript
    import { Controller, Get, Param, ParseIntPipe, Query } from '@nestjs/common';
    import { ApiOperation, ApiTags } from '@nestjs/swagger';
    import { PromptsService } from './prompts.service';
    import { GetPromptsDto } from './dto/get-prompts.dto';

    @ApiTags('prompts')
    @Controller('prompts')
    export class PromptsController {
      constructor(private readonly promptsService: PromptsService) {}

      @Get()
      @ApiOperation({ summary: '쓰기 주제 목록 조회 (필터링/페이지네이션)' })
      async findAll(@Query() dto: GetPromptsDto) {
        return this.promptsService.findAll(dto);
      }

      @Get(':id')
      @ApiOperation({ summary: '쓰기 주제 상세 조회' })
      async findOne(@Param('id', ParseIntPipe) id: number) {
        return this.promptsService.findOne(id);
      }
    }
    ```
    - X-User-Id 가드를 적용하지 않는다 (주제는 공개 데이터, Pitfall 4 방지).
    - ParseIntPipe로 id 파라미터를 정수로 자동 변환한다.
    - ResponseInterceptor가 자동으로 { success: true, data: ... } 래핑하므로 순수 데이터만 반환한다.

    **2. server/src/prompts/prompts.module.ts**:
    ```typescript
    import { Module } from '@nestjs/common';
    import { PromptsController } from './prompts.controller';
    import { PromptsService } from './prompts.service';
    import { PromptsRepository } from './prompts.repository';

    @Module({
      controllers: [PromptsController],
      providers: [PromptsService, PromptsRepository],
      exports: [PromptsService],
    })
    export class PromptsModule {}
    ```
    - PromptsService를 export하여 다른 모듈(SubmissionsModule)에서 주제 존재 확인에 사용 가능하게 한다.

    **3. server/src/app.module.ts 수정**:
    기존 imports 배열에 PromptsModule을 추가한다.
    ```typescript
    import { PromptsModule } from './prompts/prompts.module';

    @Module({
      imports: [
        ConfigModule.forRoot({ isGlobal: true }),
        DatabaseModule,
        MigrationModule,
        PromptsModule,  // 추가
      ],
      controllers: [AppController],
    })
    export class AppModule {}
    ```

  </action>
  <verify>
    <automated>cd /Users/cupist/workspace/slw && docker compose exec api sh -c "curl -s http://localhost:3000/prompts?limit=3 | head -c 500" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - server/src/prompts/prompts.controller.ts가 존재하고 "export class PromptsController" 문자열을 포함한다
    - server/src/prompts/prompts.controller.ts에 "@Controller('prompts')" 문자열이 있다
    - server/src/prompts/prompts.controller.ts에 "@Get()" 문자열이 있다
    - server/src/prompts/prompts.controller.ts에 "@Get(':id')" 문자열이 있다
    - server/src/prompts/prompts.controller.ts에 "ParseIntPipe" 문자열이 있다
    - server/src/prompts/prompts.controller.ts에 "@ApiTags('prompts')" 문자열이 있다
    - server/src/prompts/prompts.controller.ts에 "UserIdGuard" 문자열이 없다 (공개 데이터이므로 가드 미적용)
    - server/src/prompts/prompts.module.ts가 존재하고 "export class PromptsModule" 문자열을 포함한다
    - server/src/prompts/prompts.module.ts에 "exports: [PromptsService]" 문자열이 있다
    - server/src/app.module.ts에 "PromptsModule" 문자열이 있다
    - curl http://localhost:3000/prompts 응답에 "success":true가 포함된다
    - curl http://localhost:3000/prompts 응답에 "items" 배열이 포함된다
    - curl http://localhost:3000/prompts 응답에 "total" 숫자가 포함된다
    - curl http://localhost:3000/prompts?category=일기 응답에 카테고리가 일기인 항목만 포함된다
    - curl http://localhost:3000/prompts/99999 요청 시 404 에러 응답이 반환된다
  </acceptance_criteria>
  <done>GET /prompts (필터링/페이지네이션), GET /prompts/:id (상세/404) API가 동작하고, Swagger UI(/api-docs)에 prompts 태그로 문서화된 상태</done>
</task>

</tasks>

<verification>
1. GET /prompts 요청 시 시드 데이터가 페이지네이션되어 반환된다
2. GET /prompts?category=일기 요청 시 일기 카테고리만 필터링된다
3. GET /prompts?difficulty=beginner 요청 시 초급 주제만 필터링된다
4. GET /prompts?category=편지&difficulty=advanced 복합 필터링이 동작한다
5. GET /prompts/:id 요청 시 해당 주제 상세 정보가 반환된다
6. GET /prompts/99999 요청 시 404 에러가 반환된다
7. 모든 응답이 { success: true, data: ... } Envelope 형식이다
8. /api-docs에서 prompts API가 문서화되어 있다
</verification>

<success_criteria>

- GET /prompts API가 페이지네이션 + 필터링(category, difficulty) 결과를 반환한다
- GET /prompts/:id API가 상세 정보 또는 404를 반환한다
- X-User-Id 없이도 prompts API에 접근 가능하다
- TypeScript 컴파일 에러 없음
  </success_criteria>

<output>
After completion, create `.planning/phases/02-prompts-submissions/02-02-SUMMARY.md`
</output>
