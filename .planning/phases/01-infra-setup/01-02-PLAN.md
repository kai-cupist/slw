---
phase: 01-infra-setup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - server/src/migration/migration.module.ts
  - server/src/migration/migration.service.ts
  - server/src/migration/run-migrations.ts
  - server/src/common/interfaces/api-response.interface.ts
  - server/src/common/interceptors/response.interceptor.ts
  - server/src/common/filters/http-exception.filter.ts
  - server/src/main.ts
  - server/src/app.module.ts
  - server/package.json
  - migrations/000_create_schema_migrations.sql
autonomous: false

requirements:
  - INFRA-02
  - API-01
  - API-03

must_haves:
  truths:
    - "migrations/ 폴더의 SQL 파일을 순서대로 실행하면 스키마가 생성되고 schema_migrations 테이블에 이력이 기록된다"
    - "API 응답이 성공/에러 모두 Envelope 패턴({ success, data } / { success, error })으로 통일된다"
    - "유효성 검증 실패 시 400 에러로 구체적인 필드별 에러 메시지가 반환된다"
    - "GET /health 응답이 Envelope 패턴으로 감싸져 { success: true, data: { status: 'ok', database: 'connected' } } 형태로 반환된다"
  artifacts:
    - path: "migrations/000_create_schema_migrations.sql"
      provides: "마이그레이션 추적 테이블 정의"
      contains: "schema_migrations"
    - path: "server/src/migration/migration.service.ts"
      provides: "마이그레이션 러너 로직"
      exports: ["MigrationService"]
    - path: "server/src/common/interceptors/response.interceptor.ts"
      provides: "성공 응답 Envelope 래핑"
      contains: "success: true"
    - path: "server/src/common/filters/http-exception.filter.ts"
      provides: "에러 응답 Envelope 통일"
      contains: "success: false"
    - path: "server/src/common/interfaces/api-response.interface.ts"
      provides: "API 응답 타입 정의"
      exports: ["ApiSuccessResponse", "ApiErrorResponse"]
  key_links:
    - from: "server/src/main.ts"
      to: "ResponseInterceptor"
      via: "useGlobalInterceptors"
      pattern: "useGlobalInterceptors"
    - from: "server/src/main.ts"
      to: "HttpExceptionFilter"
      via: "useGlobalFilters"
      pattern: "useGlobalFilters"
    - from: "server/src/main.ts"
      to: "ValidationPipe"
      via: "useGlobalPipes"
      pattern: "useGlobalPipes"
    - from: "server/src/migration/migration.service.ts"
      to: "DatabaseService"
      via: "DI 주입"
      pattern: "DatabaseService"

user_setup: []
---

<objective>
수동 SQL 마이그레이션 시스템과 API 공통 레이어(Envelope 응답 패턴, 유효성 검증, Swagger)를 구축한다.

Purpose: 마이그레이션으로 DB 스키마를 관리하고, 모든 API가 일관된 형식으로 응답하는 기반을 확립하여 후속 Phase에서 비즈니스 로직 개발에 집중할 수 있게 한다.
Output: migration/ 모듈, common/ 공통 레이어, Swagger UI, 전역 파이프/필터/인터셉터 설정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infra-setup/01-CONTEXT.md
@.planning/phases/01-infra-setup/01-RESEARCH.md
@.planning/phases/01-infra-setup/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 핵심 인터페이스. Executor는 이 계약을 기반으로 구현한다. -->

From server/src/database/database.service.ts:
```typescript
@Injectable()
export class DatabaseService implements OnModuleDestroy {
  async query<T = any>(text: string, params?: unknown[]): Promise<T[]>;
  async queryOne<T = any>(text: string, params?: unknown[]): Promise<T | null>;
  async withTransaction<T>(callback: (client: PoolClient) => Promise<T>): Promise<T>;
  async onModuleDestroy(): Promise<void>;
}
```

From server/src/database/database.constants.ts:
```typescript
export const DATABASE_POOL = 'DATABASE_POOL';
```

From server/src/app.module.ts:
```typescript
@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    DatabaseModule,
  ],
  controllers: [AppController],
})
export class AppModule {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 수동 SQL 마이그레이션 시스템 구축</name>
  <files>
    migrations/000_create_schema_migrations.sql,
    server/src/migration/migration.module.ts,
    server/src/migration/migration.service.ts,
    server/src/migration/run-migrations.ts,
    server/src/app.module.ts,
    server/package.json
  </files>
  <read_first>
    .planning/phases/01-infra-setup/01-RESEARCH.md (패턴 3: 수동 SQL 마이그레이션 시스템)
    .planning/phases/01-infra-setup/01-CONTEXT.md (D-05 ~ D-08 마이그레이션 결정)
    server/src/app.module.ts (현재 상태)
    server/src/database/database.service.ts (DatabaseService 인터페이스 확인)
    server/package.json (현재 scripts 확인)
  </read_first>
  <action>
    1. `migrations/` 디렉토리를 프로젝트 루트에 생성하고 첫 번째 마이그레이션 파일 작성 (per D-05, D-06):

       `migrations/000_create_schema_migrations.sql`:
       ```sql
       -- 마이그레이션 이력 추적 테이블
       CREATE TABLE IF NOT EXISTS schema_migrations (
           id SERIAL PRIMARY KEY,
           filename VARCHAR(255) NOT NULL UNIQUE,
           applied_at TIMESTAMP NOT NULL DEFAULT NOW()
       );
       ```

    2. `server/src/migration/migration.service.ts` 생성 (per D-07):
       - DatabaseService를 주입받아 사용
       - `runMigrations()` 메서드:
         (a) schema_migrations 테이블 존재 확인 (없으면 000 파일로 생성)
         (b) `migrations/` 폴더에서 SQL 파일 목록을 읽어 번호순(NNN) 정렬
         (c) schema_migrations 테이블에서 이미 적용된 파일명 목록 조회
         (d) 미적용 파일만 순서대로 실행하고, 실행할 때마다 schema_migrations에 INSERT
         (e) 각 마이그레이션 적용 시 콘솔에 `[Migration] 적용: {filename}` 로그 출력
       - migrations 디렉토리 경로는 `path.join(process.cwd(), '..', 'migrations')` 사용
         (컨테이너 내부에서 server/가 workdir이므로 상위 디렉토리)
       - **주의:** 로컬 개발 시에는 `path.join(process.cwd(), 'migrations')` 일 수 있으므로,
         두 경로 중 존재하는 쪽을 사용하도록 fallback 로직 추가:
         ```typescript
         private getMigrationsDir(): string {
           const candidates = [
             path.join(process.cwd(), 'migrations'),
             path.join(process.cwd(), '..', 'migrations'),
           ];
           for (const dir of candidates) {
             if (fs.existsSync(dir)) return dir;
           }
           throw new Error('migrations 디렉토리를 찾을 수 없습니다');
         }
         ```

    3. `server/src/migration/migration.module.ts` 생성:
       ```typescript
       import { Module } from '@nestjs/common';
       import { MigrationService } from './migration.service';

       @Module({
         providers: [MigrationService],
         exports: [MigrationService],
       })
       export class MigrationModule {}
       ```

    4. `server/src/migration/run-migrations.ts` 생성 -- 독립 실행 스크립트:
       ```typescript
       import { NestFactory } from '@nestjs/core';
       import { AppModule } from '../app.module';
       import { MigrationService } from './migration.service';

       async function bootstrap() {
         const app = await NestFactory.createApplicationContext(AppModule);
         const migrationService = app.get(MigrationService);
         try {
           await migrationService.runMigrations();
           console.log('[Migration] 마이그레이션 완료');
         } catch (error) {
           console.error('[Migration] 마이그레이션 실패:', error);
           process.exit(1);
         } finally {
           await app.close();
         }
       }
       bootstrap();
       ```

    5. `server/src/app.module.ts` 수정 -- MigrationModule import 추가:
       imports 배열에 MigrationModule을 추가한다.

    6. `server/package.json`의 scripts에 마이그레이션 명령 추가:
       ```json
       "migration:run": "ts-node -r tsconfig-paths/register src/migration/run-migrations.ts"
       ```
       **주의:** tsconfig-paths가 설치되어 있는지 확인. NestJS CLI가 기본 포함하므로 보통 있음. 없으면 `npx ts-node src/migration/run-migrations.ts`로 대체.

    **핵심:** 롤백은 지원하지 않는다 (per D-08). 문제 시 새 마이그레이션으로 수정.
  </action>
  <verify>
    <automated>cd /Users/cupist/workspace/slw && test -f migrations/000_create_schema_migrations.sql && test -f server/src/migration/migration.service.ts && test -f server/src/migration/migration.module.ts && test -f server/src/migration/run-migrations.ts && grep -q "schema_migrations" migrations/000_create_schema_migrations.sql && grep -q "runMigrations" server/src/migration/migration.service.ts && grep -q "MigrationModule" server/src/app.module.ts && grep -q "migration:run" server/package.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - migrations/000_create_schema_migrations.sql 파일이 존재하고 `CREATE TABLE IF NOT EXISTS schema_migrations` SQL을 포함
    - server/src/migration/migration.service.ts에 `runMigrations` 메서드가 있음
    - migration.service.ts에서 DatabaseService를 주입받아 query 메서드를 사용함
    - migration.service.ts에서 fs.readdirSync로 migrations/ 폴더를 읽고 .sql 파일을 번호순 정렬함
    - migration.service.ts에서 schema_migrations 테이블에 적용 이력을 INSERT함
    - server/src/migration/run-migrations.ts에서 NestFactory.createApplicationContext를 사용하여 독립 실행 가능
    - server/src/app.module.ts의 imports 배열에 MigrationModule이 포함됨
    - server/package.json의 scripts에 "migration:run" 스크립트가 있음
  </acceptance_criteria>
  <done>npm run migration:run 실행 시 migrations/ 폴더의 SQL 파일이 순서대로 적용되고, schema_migrations 테이블에 이력이 기록되는 상태</done>
</task>

<task type="auto">
  <name>Task 2: API 공통 레이어 구축 (Envelope 응답, 유효성 검증, Swagger)</name>
  <files>
    server/src/common/interfaces/api-response.interface.ts,
    server/src/common/interceptors/response.interceptor.ts,
    server/src/common/filters/http-exception.filter.ts,
    server/src/main.ts
  </files>
  <read_first>
    .planning/phases/01-infra-setup/01-RESEARCH.md (패턴 4: API 응답 Envelope 패턴, main.ts 설정)
    .planning/phases/01-infra-setup/01-CONTEXT.md (D-09 ~ D-12, D-16 API 응답 결정)
    server/src/main.ts (현재 상태)
  </read_first>
  <action>
    1. `server/src/common/interfaces/api-response.interface.ts` 생성 (per D-09):
       ```typescript
       export interface ApiSuccessResponse<T> {
         success: true;
         data: T;
       }

       export interface ApiErrorResponse {
         success: false;
         error: {
           code: string;
           message: string;
           details?: any;
         };
       }

       export type ApiResponse<T> = ApiSuccessResponse<T> | ApiErrorResponse;
       ```

    2. `server/src/common/interceptors/response.interceptor.ts` 생성 (per D-10):
       ```typescript
       import {
         Injectable,
         NestInterceptor,
         ExecutionContext,
         CallHandler,
       } from '@nestjs/common';
       import { Observable } from 'rxjs';
       import { map } from 'rxjs/operators';
       import { ApiSuccessResponse } from '../interfaces/api-response.interface';

       @Injectable()
       export class ResponseInterceptor<T>
         implements NestInterceptor<T, ApiSuccessResponse<T>>
       {
         intercept(
           context: ExecutionContext,
           next: CallHandler,
         ): Observable<ApiSuccessResponse<T>> {
           return next.handle().pipe(
             map((data) => ({
               success: true as const,
               data,
             })),
           );
         }
       }
       ```

    3. `server/src/common/filters/http-exception.filter.ts` 생성 (per D-10, D-11):
       ```typescript
       import {
         ExceptionFilter,
         Catch,
         ArgumentsHost,
         HttpException,
         HttpStatus,
       } from '@nestjs/common';
       import { Response } from 'express';

       @Catch(HttpException)
       export class HttpExceptionFilter implements ExceptionFilter {
         catch(exception: HttpException, host: ArgumentsHost) {
           const ctx = host.switchToHttp();
           const response = ctx.getResponse<Response>();
           const status = exception.getStatus();
           const exceptionResponse = exception.getResponse();

           const errorMessage =
             typeof exceptionResponse === 'string'
               ? exceptionResponse
               : (exceptionResponse as any).message;

           response.status(status).json({
             success: false,
             error: {
               code: HttpStatus[status] || 'UNKNOWN_ERROR',
               message: Array.isArray(errorMessage)
                 ? errorMessage.join(', ')
                 : errorMessage,
               ...(Array.isArray(errorMessage) && { details: errorMessage }),
             },
           });
         }
       }
       ```

    4. `server/src/main.ts` 수정 -- 전역 파이프/인터셉터/필터/Swagger 설정 (per D-16):
       ```typescript
       import { NestFactory } from '@nestjs/core';
       import { ValidationPipe } from '@nestjs/common';
       import { SwaggerModule, DocumentBuilder } from '@nestjs/swagger';
       import { AppModule } from './app.module';
       import { ResponseInterceptor } from './common/interceptors/response.interceptor';
       import { HttpExceptionFilter } from './common/filters/http-exception.filter';

       async function bootstrap() {
         const app = await NestFactory.create(AppModule);

         // 전역 ValidationPipe (per D-11, API-03)
         app.useGlobalPipes(
           new ValidationPipe({
             whitelist: true,
             forbidNonWhitelisted: true,
             transform: true,
           }),
         );

         // 전역 인터셉터 (per D-10) - 성공 응답을 { success: true, data } 로 래핑
         app.useGlobalInterceptors(new ResponseInterceptor());

         // 전역 필터 (per D-10) - 에러 응답을 { success: false, error } 로 통일
         app.useGlobalFilters(new HttpExceptionFilter());

         // Swagger 설정
         const config = new DocumentBuilder()
           .setTitle('말하기 듣기 쓰기 API')
           .setDescription('쓰기 평가 학습 앱 백엔드 API')
           .setVersion('1.0')
           .build();
         const document = SwaggerModule.createDocument(app, config);
         SwaggerModule.setup('api-docs', app, document);

         await app.listen(3000);
       }
       bootstrap();
       ```

    **핵심 체크포인트:**
    - ValidationPipe에 whitelist: true, forbidNonWhitelisted: true, transform: true 설정 (per D-11)
    - HttpExceptionFilter에서 ValidationPipe 에러의 message 배열을 details 필드로 변환 (per RESEARCH.md Pitfall 5)
    - Swagger UI는 /api-docs 경로에서 접근 가능
  </action>
  <verify>
    <automated>cd /Users/cupist/workspace/slw && test -f server/src/common/interfaces/api-response.interface.ts && test -f server/src/common/interceptors/response.interceptor.ts && test -f server/src/common/filters/http-exception.filter.ts && grep -q "success: true" server/src/common/interceptors/response.interceptor.ts && grep -q "success: false" server/src/common/filters/http-exception.filter.ts && grep -q "useGlobalPipes" server/src/main.ts && grep -q "useGlobalInterceptors" server/src/main.ts && grep -q "useGlobalFilters" server/src/main.ts && grep -q "SwaggerModule" server/src/main.ts && grep -q "whitelist" server/src/main.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - server/src/common/interfaces/api-response.interface.ts에 ApiSuccessResponse, ApiErrorResponse, ApiResponse 타입이 export됨
    - ApiSuccessResponse에 `success: true`와 `data: T` 필드가 있음
    - ApiErrorResponse에 `success: false`와 `error: { code, message, details? }` 필드가 있음
    - response.interceptor.ts에서 `success: true as const`와 `data`를 포함하는 객체로 래핑함
    - http-exception.filter.ts에서 `success: false`와 `error` 객체를 반환함
    - http-exception.filter.ts에서 Array.isArray(errorMessage) 체크로 ValidationPipe 에러의 details를 포함함
    - main.ts에 useGlobalPipes(new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true }))가 있음
    - main.ts에 useGlobalInterceptors(new ResponseInterceptor())가 있음
    - main.ts에 useGlobalFilters(new HttpExceptionFilter())가 있음
    - main.ts에 SwaggerModule.setup('api-docs', ...)이 있음
  </acceptance_criteria>
  <done>모든 API 응답이 Envelope 패턴으로 통일되고, 유효성 검증/에러 처리가 전역 설정되며, /api-docs에서 Swagger UI가 동작하는 상태</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 전체 통합 검증</name>
  <files>docker-compose.yml, server/src/main.ts</files>
  <action>
    Executor가 docker compose up --build -d를 실행하고, 마이그레이션을 수동 실행한 뒤, 아래 자동 검증 명령을 실행하여 결과를 사용자에게 보여준다.
    사용자는 결과를 확인하고 Swagger UI(http://localhost:3000/api-docs)를 브라우저에서 직접 확인한다.
  </action>
  <what-built>
    Plan 01(Docker Compose + DatabaseModule)과 Plan 02(마이그레이션 + API 공통 레이어)의 전체 결과물.
    docker compose up으로 PostgreSQL과 NestJS 앱이 실행되고, 마이그레이션이 적용되며, API 응답이 Envelope 패턴으로 통일된 상태.
  </what-built>
  <how-to-verify>
    1. Docker 환경 실행:
       ```bash
       cd /Users/cupist/workspace/slw
       docker compose up --build -d
       ```
       PostgreSQL(db)과 NestJS(api) 컨테이너가 모두 실행 중인지 확인:
       ```bash
       docker compose ps
       ```
       **예상:** 두 컨테이너 모두 "running" 또는 "Up" 상태

    2. 마이그레이션 실행:
       ```bash
       docker compose exec api npm run migration:run
       ```
       **예상:** `[Migration] 적용: 000_create_schema_migrations.sql` 출력 후 `[Migration] 마이그레이션 완료`

    3. 마이그레이션 중복 실행 테스트:
       ```bash
       docker compose exec api npm run migration:run
       ```
       **예상:** 이미 적용된 마이그레이션이므로 아무 파일도 적용하지 않고 `[Migration] 마이그레이션 완료` 출력

    4. 헬스체크 API (Envelope 응답 확인):
       ```bash
       curl -s http://localhost:3000/health | jq
       ```
       **예상:** `{ "success": true, "data": { "status": "ok", "database": "connected" } }`

    5. 존재하지 않는 경로 (에러 Envelope 확인):
       ```bash
       curl -s http://localhost:3000/nonexistent | jq
       ```
       **예상:** `{ "success": false, "error": { "code": "NOT_FOUND", "message": "Cannot GET /nonexistent" } }`

    6. Swagger UI 확인:
       브라우저에서 http://localhost:3000/api-docs 접속
       **예상:** Swagger UI가 표시되고 GET /health 엔드포인트가 목록에 있음

    7. DB에서 schema_migrations 테이블 확인:
       ```bash
       docker compose exec db psql -U slw_user -d slw -c "SELECT * FROM schema_migrations;"
       ```
       **예상:** 000_create_schema_migrations.sql이 한 행으로 기록됨
  </how-to-verify>
  <verify>
    <automated>cd /Users/cupist/workspace/slw && docker compose ps 2>/dev/null && curl -sf http://localhost:3000/health && echo " PASS"</automated>
  </verify>
  <done>사용자가 7개 검증 항목을 확인하고 "approved"를 입력하면 Phase 1 완료</done>
  <resume-signal>"approved"를 입력하거나, 발견된 문제를 설명해주세요</resume-signal>
</task>

</tasks>

<verification>
1. `docker compose up --build -d` 후 `docker compose ps`에서 db, api 모두 running
2. `docker compose exec api npm run migration:run` 실행 시 000 파일 적용 로그 출력
3. `curl http://localhost:3000/health` 응답이 `{ "success": true, "data": { "status": "ok", "database": "connected" } }`
4. `curl http://localhost:3000/nonexistent` 응답이 `{ "success": false, "error": { "code": "NOT_FOUND", ... } }`
5. http://localhost:3000/api-docs에서 Swagger UI 접근 가능
6. `docker compose exec db psql -U slw_user -d slw -c "SELECT * FROM schema_migrations;"` 에서 적용 이력 확인
</verification>

<success_criteria>
- migrations/ 폴더의 SQL 파일이 순서대로 실행되고 schema_migrations에 이력이 기록됨
- 마이그레이션 중복 실행 시 이미 적용된 파일은 건너뜀
- 모든 API 성공 응답이 { success: true, data: T } 형식
- 모든 API 에러 응답이 { success: false, error: { code, message } } 형식
- ValidationPipe가 전역 설정되어 whitelist + forbidNonWhitelisted + transform이 활성화됨
- Swagger UI가 /api-docs에서 접근 가능
</success_criteria>

<output>
완료 후 `.planning/phases/01-infra-setup/01-02-SUMMARY.md`를 생성한다.
</output>
