---
phase: 01-infra-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/package.json
  - server/tsconfig.json
  - server/nest-cli.json
  - server/src/main.ts
  - server/src/app.module.ts
  - server/src/app.controller.ts
  - server/src/database/database.module.ts
  - server/src/database/database.service.ts
  - server/src/database/database.constants.ts
  - docker-compose.yml
  - Dockerfile
  - .env
  - .env.example
  - .gitignore
  - .dockerignore
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03
  - INFRA-04
  - API-02

must_haves:
  truths:
    - "docker compose up 한 번으로 PostgreSQL과 NestJS 앱이 실행된다"
    - "NestJS 앱이 PostgreSQL에 Raw SQL(pg Pool)로 연결되어 쿼리를 실행할 수 있다"
    - "환경 변수(.env)로 DB 접속 정보가 관리되고, .env.example이 존재한다"
    - "DatabaseService가 query, queryOne, withTransaction 메서드를 제공한다"
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostgreSQL + NestJS 컨테이너 정의"
      contains: "service_healthy"
    - path: "Dockerfile"
      provides: "NestJS 앱 개발 빌드"
      contains: "node:20-alpine"
    - path: ".env.example"
      provides: "환경 변수 템플릿"
      contains: "DB_HOST"
    - path: "server/src/database/database.module.ts"
      provides: "Global DatabaseModule"
      contains: "@Global"
    - path: "server/src/database/database.service.ts"
      provides: "Raw SQL 쿼리 래퍼"
      exports: ["DatabaseService"]
  key_links:
    - from: "docker-compose.yml"
      to: ".env"
      via: "env_file 로드"
      pattern: "env_file.*\\.env"
    - from: "server/src/database/database.module.ts"
      to: "pg Pool"
      via: "ConfigService에서 DB 설정 주입"
      pattern: "DATABASE_POOL"
    - from: "server/src/app.module.ts"
      to: "DatabaseModule"
      via: "imports 배열"
      pattern: "DatabaseModule"
---

<objective>
NestJS 프로젝트를 생성하고, Docker Compose로 PostgreSQL + NestJS 개발 환경을 구성하며, DatabaseModule(pg Pool 기반)을 구축한다.

Purpose: 모든 후속 기능 개발의 인프라 기반을 마련한다. docker compose up 한 번으로 DB 연결이 완료된 NestJS 앱이 실행되는 상태를 달성한다.
Output: server/ 디렉토리(NestJS 프로젝트), Docker Compose 설정, DatabaseModule, 환경 변수 관리
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infra-setup/01-CONTEXT.md
@.planning/phases/01-infra-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NestJS 프로젝트 생성 + Docker Compose + 환경 변수 설정</name>
  <files>
    server/package.json, server/tsconfig.json, server/nest-cli.json,
    server/src/main.ts, server/src/app.module.ts, server/src/app.controller.ts,
    docker-compose.yml, Dockerfile, .env, .env.example, .gitignore, .dockerignore
  </files>
  <read_first>
    .planning/phases/01-infra-setup/01-RESEARCH.md (패턴 1: Docker Compose 개발 환경, .env 설정, Dockerfile)
    .planning/phases/01-infra-setup/01-CONTEXT.md (D-01 ~ D-04 잠긴 결정)
    CLAUDE.md (프로젝트 제약사항 확인)
  </read_first>
  <action>
    1. NestJS CLI로 프로젝트 생성 (per D-13, 재량: npm 사용):
       ```bash
       npx @nestjs/cli new server --strict --package-manager npm
       ```

    2. Phase 1 핵심 의존성 설치 (per RESEARCH.md Standard Stack):
       ```bash
       cd server
       npm install pg @nestjs/config @nestjs/swagger class-validator class-transformer
       npm install -D @types/pg
       ```

    3. `.env` 파일 생성 (per D-04):
       ```env
       DB_HOST=db
       DB_PORT=5432
       DB_NAME=slw
       DB_USER=slw_user
       DB_PASSWORD=slw_password_change_me
       API_PORT=3000
       NODE_ENV=development
       ```

    4. `.env.example` 파일 생성 (per D-04, .env와 동일 내용):
       ```env
       DB_HOST=db
       DB_PORT=5432
       DB_NAME=slw
       DB_USER=slw_user
       DB_PASSWORD=slw_password_change_me
       API_PORT=3000
       NODE_ENV=development
       ```

    5. `.gitignore` 업데이트 — 기존 server/.gitignore를 프로젝트 루트로 이동/통합하고 다음 항목 추가:
       ```
       .env
       node_modules/
       dist/
       ```
       **주의:** `.env.example`은 gitignore에 포함하지 않는다.

    6. `Dockerfile` 생성 (프로젝트 루트, per D-01 hot reload 지원):
       ```dockerfile
       FROM node:20-alpine AS development
       WORKDIR /usr/src/app
       COPY --chown=node:node package*.json ./
       RUN npm ci
       COPY --chown=node:node . .
       USER node
       ```

    7. `.dockerignore` 생성:
       ```
       node_modules
       dist
       .git
       .env
       ```

    8. `docker-compose.yml` 생성 (per D-01, D-02, D-03):
       ```yaml
       services:
         db:
           image: postgres:16-alpine
           restart: unless-stopped
           environment:
             POSTGRES_DB: ${DB_NAME}
             POSTGRES_USER: ${DB_USER}
             POSTGRES_PASSWORD: ${DB_PASSWORD}
           ports:
             - "${DB_PORT:-5432}:5432"
           volumes:
             - pgdata:/var/lib/postgresql/data
           healthcheck:
             test: ["CMD", "pg_isready", "-U", "${DB_USER}", "-d", "${DB_NAME}"]
             interval: 5s
             timeout: 5s
             retries: 5
             start_period: 10s

         api:
           build:
             context: ./server
             dockerfile: ../Dockerfile
             target: development
           volumes:
             - ./server:/usr/src/app
             - /usr/src/app/node_modules
           env_file:
             - .env
           ports:
             - "${API_PORT:-3000}:3000"
           depends_on:
             db:
               condition: service_healthy
           command: npm run start:dev

       volumes:
         pgdata:
       ```

    9. `server/src/app.module.ts` 수정 — ConfigModule.forRoot()를 isGlobal: true로 등록 (per D-04):
       ```typescript
       import { Module } from '@nestjs/common';
       import { ConfigModule } from '@nestjs/config';
       import { AppController } from './app.controller';

       @Module({
         imports: [
           ConfigModule.forRoot({
             isGlobal: true,
           }),
         ],
         controllers: [AppController],
       })
       export class AppModule {}
       ```

    **주의사항:**
    - Docker Compose에서 `condition: service_healthy`를 반드시 사용한다 (RESEARCH.md Pitfall 1 참고)
    - node_modules anonymous volume으로 호스트/컨테이너 충돌 방지
    - NestJS CLI가 생성한 불필요한 파일(app.service.ts, app.controller.spec.ts 등)은 정리한다
  </action>
  <verify>
    <automated>cd /Users/cupist/workspace/slw && test -f docker-compose.yml && test -f Dockerfile && test -f .env && test -f .env.example && test -f server/package.json && grep -q "pg" server/package.json && grep -q "service_healthy" docker-compose.yml && grep -q "@nestjs/config" server/package.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml에 `condition: service_healthy`가 포함됨
    - docker-compose.yml에 `pgdata` 볼륨과 `/usr/src/app/node_modules` anonymous volume이 정의됨
    - Dockerfile에 `FROM node:20-alpine AS development`가 포함됨
    - .env에 DB_HOST=db, DB_PORT=5432, DB_NAME=slw, DB_USER=slw_user가 있음
    - .env.example이 존재하고 동일한 키를 포함
    - .gitignore에 `.env`가 포함되고 `.env.example`은 포함되지 않음
    - server/package.json에 pg, @nestjs/config, @nestjs/swagger, class-validator, class-transformer 의존성이 있음
    - server/src/app.module.ts에 `ConfigModule.forRoot`와 `isGlobal: true`가 포함됨
  </acceptance_criteria>
  <done>NestJS 프로젝트가 server/ 디렉토리에 생성되고, Docker Compose 설정이 완료되어 `docker compose up`으로 PostgreSQL + NestJS 앱이 실행 가능한 상태</done>
</task>

<task type="auto">
  <name>Task 2: DatabaseModule 구축 (Global pg Pool + DatabaseService)</name>
  <files>
    server/src/database/database.module.ts,
    server/src/database/database.service.ts,
    server/src/database/database.constants.ts,
    server/src/app.module.ts,
    server/src/app.controller.ts
  </files>
  <read_first>
    .planning/phases/01-infra-setup/01-RESEARCH.md (패턴 2: DatabaseModule, 패턴 5: 헬스체크)
    server/src/app.module.ts (현재 상태 확인)
    server/src/app.controller.ts (현재 상태 확인)
  </read_first>
  <action>
    1. `server/src/database/database.constants.ts` 생성 (per D-14):
       ```typescript
       export const DATABASE_POOL = 'DATABASE_POOL';
       ```

    2. `server/src/database/database.module.ts` 생성 (per D-14, @Global 모듈):
       ```typescript
       import { Global, Module } from '@nestjs/common';
       import { ConfigModule, ConfigService } from '@nestjs/config';
       import { Pool } from 'pg';
       import { DATABASE_POOL } from './database.constants';
       import { DatabaseService } from './database.service';

       @Global()
       @Module({
         imports: [ConfigModule],
         providers: [
           {
             provide: DATABASE_POOL,
             inject: [ConfigService],
             useFactory: (configService: ConfigService) => {
               const pool = new Pool({
                 host: configService.get('DB_HOST'),
                 port: configService.get<number>('DB_PORT'),
                 user: configService.get('DB_USER'),
                 password: configService.get('DB_PASSWORD'),
                 database: configService.get('DB_NAME'),
                 max: 10,
                 idleTimeoutMillis: 30000,
                 connectionTimeoutMillis: 5000,
               });
               return pool;
             },
           },
           DatabaseService,
         ],
         exports: [DatabaseService],
       })
       export class DatabaseModule {}
       ```

    3. `server/src/database/database.service.ts` 생성 (per D-12, D-14, RESEARCH.md 패턴 2):
       ```typescript
       import { Inject, Injectable, OnModuleDestroy } from '@nestjs/common';
       import { Pool, PoolClient } from 'pg';
       import { DATABASE_POOL } from './database.constants';

       @Injectable()
       export class DatabaseService implements OnModuleDestroy {
         constructor(@Inject(DATABASE_POOL) private readonly pool: Pool) {}

         async query<T = any>(text: string, params?: unknown[]): Promise<T[]> {
           const result = await this.pool.query(text, params);
           return result.rows;
         }

         async queryOne<T = any>(text: string, params?: unknown[]): Promise<T | null> {
           const result = await this.pool.query(text, params);
           return result.rows[0] ?? null;
         }

         async withTransaction<T>(
           callback: (client: PoolClient) => Promise<T>,
         ): Promise<T> {
           const client = await this.pool.connect();
           try {
             await client.query('BEGIN');
             const result = await callback(client);
             await client.query('COMMIT');
             return result;
           } catch (error) {
             await client.query('ROLLBACK');
             throw error;
           } finally {
             client.release();
           }
         }

         async onModuleDestroy() {
           await this.pool.end();
         }
       }
       ```

    4. `server/src/app.module.ts` 수정 — DatabaseModule import 추가:
       ```typescript
       import { Module } from '@nestjs/common';
       import { ConfigModule } from '@nestjs/config';
       import { DatabaseModule } from './database/database.module';
       import { AppController } from './app.controller';

       @Module({
         imports: [
           ConfigModule.forRoot({ isGlobal: true }),
           DatabaseModule,
         ],
         controllers: [AppController],
       })
       export class AppModule {}
       ```

    5. `server/src/app.controller.ts` 수정 — 헬스체크 엔드포인트 추가 (per 재량: 직접 구현):
       ```typescript
       import { Controller, Get, ServiceUnavailableException } from '@nestjs/common';
       import { DatabaseService } from './database/database.service';

       @Controller()
       export class AppController {
         constructor(private readonly db: DatabaseService) {}

         @Get('health')
         async healthCheck() {
           try {
             await this.db.query('SELECT 1');
             return { status: 'ok', database: 'connected' };
           } catch {
             throw new ServiceUnavailableException('데이터베이스 연결 실패');
           }
         }
       }
       ```

    **핵심 체크포인트:**
    - connectionTimeoutMillis: 5000 반드시 설정 (RESEARCH.md Pitfall 2)
    - onModuleDestroy에서 pool.end() 호출 (graceful shutdown)
    - SQL 파라미터 바인딩 패턴 ($1, $2) 강제 — query/queryOne 메서드가 params 배열을 받음 (per D-12, API-02)
    - withTransaction에서 finally 블록의 client.release() 필수
  </action>
  <verify>
    <automated>cd /Users/cupist/workspace/slw && test -f server/src/database/database.module.ts && test -f server/src/database/database.service.ts && test -f server/src/database/database.constants.ts && grep -q "@Global" server/src/database/database.module.ts && grep -q "DATABASE_POOL" server/src/database/database.constants.ts && grep -q "connectionTimeoutMillis" server/src/database/database.service.ts || grep -q "connectionTimeoutMillis" server/src/database/database.module.ts && grep -q "onModuleDestroy" server/src/database/database.service.ts && grep -q "DatabaseModule" server/src/app.module.ts && grep -q "health" server/src/app.controller.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - server/src/database/database.constants.ts에 `DATABASE_POOL` 상수가 export됨
    - server/src/database/database.module.ts에 `@Global()` 데코레이터와 `DATABASE_POOL` 프로바이더가 있음
    - server/src/database/database.module.ts에 `connectionTimeoutMillis: 5000`이 설정됨
    - server/src/database/database.service.ts에 query, queryOne, withTransaction 메서드가 있음
    - server/src/database/database.service.ts에 onModuleDestroy에서 pool.end()가 호출됨
    - server/src/database/database.service.ts에 withTransaction의 finally 블록에서 client.release()가 호출됨
    - server/src/app.module.ts에 DatabaseModule이 imports 배열에 포함됨
    - server/src/app.controller.ts에 GET /health 엔드포인트가 있고 DatabaseService를 주입받아 SELECT 1로 DB 연결을 확인함
  </acceptance_criteria>
  <done>DatabaseModule이 @Global 모듈로 등록되어 어디서든 DatabaseService를 주입할 수 있고, GET /health 엔드포인트가 DB 연결을 확인하여 응답하는 상태</done>
</task>

</tasks>

<verification>
1. `docker compose up --build -d` 실행 후 두 컨테이너(db, api)가 healthy/running 상태인지 확인
2. `curl http://localhost:3000/health` 응답이 `{ "status": "ok", "database": "connected" }`인지 확인
3. .env.example 파일이 존재하고 DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD 키를 포함하는지 확인
4. server/src/database/database.service.ts에 query, queryOne, withTransaction 메서드가 존재하는지 확인
</verification>

<success_criteria>
- docker compose up 한 번으로 PostgreSQL + NestJS 앱이 실행되고 서로 연결됨
- GET /health 엔드포인트가 DB 연결 상태를 포함하여 응답
- DatabaseService가 query/queryOne/withTransaction 메서드를 전역 제공
- 환경 변수(.env)로 모든 설정이 관리되고 .env.example이 존재
- SQL 파라미터 바인딩 패턴이 DatabaseService 인터페이스에 강제됨
</success_criteria>

<output>
완료 후 `.planning/phases/01-infra-setup/01-01-SUMMARY.md`를 생성한다.
</output>
